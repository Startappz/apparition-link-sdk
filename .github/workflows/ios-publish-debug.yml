# Publish a build for development. The Xcode framework is debuggable.
name: ios-publish-debug
on:
  workflow_dispatch:

permissions:
  contents: write
  packages: write

jobs:
  call-publish:
    uses: ./.github/workflows/ios-base-publish.yml
    with:
      build-debug: true